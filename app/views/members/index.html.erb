<h1><%= @list.name %></h1>
<br />

<h3>Listing members</h3>

<table class="table table-striped">
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Email</th>
    <th>Create Date</th>
    <th>Action</th>
  </tr>

  <%= render @members if @members %>

</table>

<br />

<%= link_to 'New Member', new_list_member_path(@list.id) %>
